from recordProcessor import RecordProcessor

EMPTY_STRING = ""


class RecordCompactor(RecordProcessor):

    def __init__(self):
        pass
    
    def compact(self, payload):
        pass
    
    def processRecord(record):
        return record.getHeader(), compact(record.getPayload())) # StringRecord

